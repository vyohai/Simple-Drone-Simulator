#include <string>
#include <iostream>
#include <cmath>

#include <sciplot/sciplot.hpp>

#include "visualization.hpp"

void createFigureFiles()
{
    sciplot::Vec x = sciplot::linspace(0.0, sciplot::PI, 200);

}
sciplot::Plot2D singlePlot(int axis,float angle)
{
    sciplot::Vec x = sciplot::linspace(0.0, sciplot::PI, 200);
    sciplot::Plot2D plot2d;
    return plot2d;
}